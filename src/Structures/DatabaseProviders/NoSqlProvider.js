/**
* Provider using the `wio.db` library.
* @param {any} db  - A json database path.
*/
class NoSqlProvider {
    constructor(db) {
        this.db = db;
    }
    // set(id,datas) {

    // }
}

module.exports = NoSqlProvider;
